name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: https://gitea.huiyim.cn/actions/checkout@v4

      - name: Setup Node.js
        uses: https://gitea.huiyim.cn/actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        run: npm install -g pnpm@9

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Transfer Files to Server
        uses: https://gitea.huiyim.cn/actions/scp-action@v0.1.7
        with:
          host: "202.200.206.157"
          username: "root"
          password: "Gaohp@990421"
          source: ".output/,docker-compose.yml,Dockerfile,package.json"
          target: "/data/prompt"

      - name: Deploy Service
        uses: https://gitea.huiyim.cn/actions/ssh-action@v1.0.0
        with:
          host: "202.200.206.157"
          username: "root"
          password: "Gaohp@990421"
          script: |
            cd /data/prompt

            echo "🛑 停止并清理旧容器（等待30秒）..."
            docker compose down -t 30

            echo "🚀 启动新服务..."
            docker compose up -d --build --remove-orphans --force-recreate

            echo "🚀 发布完成，等待容器重启..."
